# 0919 模型状态

尚未生成 0919.rknn：当前机器没有可用的 Linux/WSL、Docker 或 RKNN-Toolkit2 转换环境。

0919.onnx 已复制到本目录，ONNX checker 校验通过。项目转换目标为 RK3588，RGB 输入，mean=[0,0,0]、std=[255,255,255]。默认生成非量化模型，无需校准集。

在安装了兼容版本 onnx 和 rknn-toolkit2 的 Linux 环境中，从本目录执行：

```bash
bash convert_0919.sh
```

成功后输出到本目录的 0919.rknn。转换脚本不会默认覆盖已有输出。

## 接入差异

新模型输入 [1,3,640,640]，输出 [1,15,8400]，11 类顺序如下：

```text
chocks, extinguisher, plate, tag, light, support, screw, tank,
lamp_broken, box_broken, warning
```

现有 rtsp_truck.py 的 truck10 配置要求 [1,14,8400]，其类别顺序与新模型不同。不能直接用新模型覆盖 best_truck.rknn；需同步适配类别表、中文标签和下游类别使用。新模型不含 tyre 和 truck，无法保留依赖这两个类别的识别功能，仅修改标签不能恢复这些能力。

现有配置和原模型未修改。实际导出后还需要在 RK3588 板端验证加载、runtime/驱动兼容性及真实图片检测结果。

完整检查结果见 0919_model_info.json。
